
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Submit a Note</title>
</head>
<body>
  <h1>Edit a Note</h1>
  <form action="/notes/update/<%= noteID%>" method="POST">
    
    <label for="title">Note Title:</label><br>
    <input type="text" id="title" name="title" value="<%= note.title%>" required><br><br>

    <label for="content">Note Content:</label><br>
    <textarea id="content" name="content" rows="10" cols="50" required><%= note.content%></textarea><br><br>

    <label for="category">Category:</label>
                <select name="category">
                  <option value="work" selected="<%=note.category === 'work'%>">Work</option>
                  <option value="personal" selected="<%=note.category === 'personal'%>">Personal</option>
                  <option value="ideas" selected="<%=note.category === 'ideas'%>">Ideas</option>
                  <option value="todos" selected="<%=note.category === 'todos'%>">TODOs</option>
                <option value="other" selected="<%=note.category === 'other'%>">Other</option>
</select>


    <button type="submit" class="btn btn-primary">Save</button>
  </form>
</body>
</html>